import TicketService from "../src/pairtest/TicketService"

describe("", () => {
    test("", () => {
        const actual =  new TicketService;
        console.log(actual.purchaseTickets(1, []));
        
    })
})