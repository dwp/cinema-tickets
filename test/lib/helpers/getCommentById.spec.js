import { expect } from "chai";

const getCommentById = (req, res, next) => {
    return undefined;
}

describe.only('getCommentById', () => {
    
    it('should call next spy', () => {  
        
    });

    it('should set res.comment', () => {  

    });

    it('should fail when null is returned', () => {  

    });
});